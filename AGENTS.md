# Unity UI 구현 기준

- 반복되거나 재사용될 UI 단위는 적극적으로 프리팹으로 만든다.
- 룰 카드, 슬롯, 아이콘 버튼, 보드 셀, 결과 표시처럼 같은 구조가 반복되는 요소는 프리팹 우선으로 구현한다.
- 정적 UI는 반드시 씬 또는 프리팹에 정의한다.
- 코드로 정적 레이아웃, 패널, 버튼, 텍스트, 장식 요소를 새로 생성하지 않는다.
- 코드는 씬/프리팹에 배치된 UI 참조를 받아 상태만 갱신한다.
- 동적 UI 생성은 런타임 데이터 개수가 변하는 영역에만 허용한다.
- 동적 UI가 필요한 경우에도 컨테이너, 프리팹, 스타일 기준은 씬/프리팹에 둔다.
- 동적 UI도 가능하면 코드로 GameObject를 조립하지 말고 프리팹을 Instantiate해서 채운다.
- 동적 생성 대상 예시는 보드 셀, 스테이지별 룰 슬롯, 결과 하이라이트처럼 데이터에 따라 개수나 내용이 달라지는 요소다.
- 고정 대상 예시는 화면 루트, 좌측 패널, 우측 보드 영역, 상단 내비게이션, 실행 버튼 영역, 기본 간격과 색상 구조다.
- 씬/프리팹 기반 UI와 코드 생성 UI가 충돌하면 씬/프리팹 기준으로 통일한다.
- UI 수정 후에는 실제 Game 화면 캡처로 확인한다.
